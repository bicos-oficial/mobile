name: bicos
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^0.1.3
  mask_text_input_formatter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.8.0"


flutter:

  uses-material-design: true

  assets:

    - images/logo.png
    - images/user.png
    - images/bicos_logo_sombra.png
    - images/bicos_logo.png

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "images/bicos_logo.psd"